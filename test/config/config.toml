[[profile]]
    name = "stage"
    type = "mysql"
    hostname = "{$file::stage_config.json/deep.config.why.because.db.hostname}"
    username = "{$file::stage_config.json/deep.config.why.because.db.username}"
    password = "{$file::stage_config.json/deep.config.why.because.db.password}"

[[profile]]
    name = "prod"
    type = "mysql"
    hostname = "{$file::prod_config.yaml/some.other.key.hostname}"
    username = "{$file::prod_config.yaml/a.different.username}"
    password = "{$file::prod_config.yaml/a.different.password}"
